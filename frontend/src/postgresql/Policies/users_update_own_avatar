CREATE POLICY "Users update own avatar"
ON storage.objects
FOR UPDATE
TO authenticated
USING (
    bucket_id = 'Avatars'
    AND (storage.foldername(name))[1] = auth.uid()::text
)
WITH CHECK (
    bucket_id = 'Avatars'
    AND (storage.foldername(name))[1] = auth.uid()::text
);